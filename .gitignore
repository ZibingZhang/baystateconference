_site
.claude
.DS_Store
.sass-cache
.jekyll-cache
.jekyll-metadata
scripts
vendor
__pycache__
*.pyc
.venv

# Daily-fetched ArbiterLive schedule data (see arbiterlive/arbiter_schedule.py).
# Cached via GitHub Actions between the fetch and build workflows rather than
# committed - see .github/workflows/fetch-schedule.yaml for how CI produces
# it, and README.md for how to regenerate it locally.
/jekyll/_data/schedule/
